info:
  name: GetAllUsers
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/admin/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
