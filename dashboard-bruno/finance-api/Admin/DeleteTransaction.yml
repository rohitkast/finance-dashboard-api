info:
  name: DeleteTransaction
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/admin/transaction/:uid"
  params:
    - name: uid
      value: "5"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
