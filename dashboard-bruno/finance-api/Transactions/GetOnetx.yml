info:
  name: GetOnetx
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/transaction/:id"
  params:
    - name: id
      value: "19"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
