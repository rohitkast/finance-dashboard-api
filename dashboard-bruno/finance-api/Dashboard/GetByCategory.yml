info:
  name: GetByCategory
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/dashboard/category/:category"
  params:
    - name: category
      value: income
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
