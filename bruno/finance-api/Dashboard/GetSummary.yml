info:
  name: GetSummary
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/dashboard/summary"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
