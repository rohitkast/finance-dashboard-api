info:
  name: DeleteUser
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/admin/user/:uid"
  params:
    - name: uid
      value: "7"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
