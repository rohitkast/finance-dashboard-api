info:
  name: GetUserTx
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/admin/transactions/:uid"
  params:
    - name: uid
      value: "5"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
