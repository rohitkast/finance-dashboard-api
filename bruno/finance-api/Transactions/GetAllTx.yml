info:
  name: GetAllTx
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/transaction/all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
