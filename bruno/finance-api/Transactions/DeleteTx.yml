info:
  name: DeleteTx
  type: http
  seq: 1

http:
  method: DELETE
  url: "{{baseUrl}}/transaction/:id"
  params:
    - name: id
      value: "19"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
