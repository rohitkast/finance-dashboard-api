info:
  name: Updatetx
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/transaction/:id"
  params:
    - name: id
      value: "3"
      type: path
  body:
    type: json
    data: |-
      {
        "Amount": 200, 
        "Description": "my first transaction",
        "Category": "expense"   
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
